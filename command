when i play tclient in this configuration it works perfectly fine in windowed mode and hdr is active but when i switch tclient to fullscreen the monitor restarts with hdr disabled. when i switch back to windowed or close the game the monitor restarts again and starts with hdr active again. in this git repo https://github.com/Tiizzel/HyprNix when i play tclient on that config that doesnt happen, even in fullscreen. can you check why that happens? start with the latest logs from tclient. research why that problem occurs and then report back, without changing anything yet
